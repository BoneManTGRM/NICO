# Assessment recovery proof

Recovery is valid only when the frontend reloads the original run ID, observes the same repository and exact commit, preserves or advances revision, and continues the existing lifecycle without a second intake request.
