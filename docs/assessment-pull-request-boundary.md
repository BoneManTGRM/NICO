# Assessment pull request boundary

The draft pull request must summarize root cause, changes, tests, deployment actions, and rollback considerations. It must not claim production persistence is fixed until the deployment is configured.
