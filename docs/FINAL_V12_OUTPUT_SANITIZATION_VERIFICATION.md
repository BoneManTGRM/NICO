# Final v12 output verification

The rebuilt Markdown, HTML, and PDF outputs must contain no client-visible unavailable complexity measurements expressed as `None`.

The final response must preserve the original maturity score, evidence provenance, historical reliability findings, human-review requirement, and report-only no-write boundary.
