# Report readiness gate API route

Endpoint:

`POST /reports/readiness-gate`

Module:

`nico/report_readiness_gate_api.py`

The route returns `nico.report_readiness_gate.v1` from a supplied payload.

Use it before generating or trusting a fresh Express report.
