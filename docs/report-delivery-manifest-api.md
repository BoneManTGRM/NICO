# Report delivery manifest API route

Endpoint:

`POST /reports/delivery-manifest`

Module:

`nico/report_delivery_manifest_api.py`

The route returns `nico.report_delivery_manifest.v1` from a supplied payload.

Use it to produce a single report delivery manifest before sending a client report.
