# Observed production evidence

The active Comprehensive run showed a durable record as `Not verified` after continuation, rendered the running dependency/security stage ahead of completed immutable-snapshot prerequisites, and displayed `Not verified` beneath a completed authorization stage. These conditions are presentation and response-contract defects; they do not alter the immutable run identity.
