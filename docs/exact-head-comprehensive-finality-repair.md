# Exact-head Comprehensive finality repair

## Authoritative trigger

Default branch commit `1eba1fbdf5f38d9a8be863548584e7ffbe1d5ce2` deployed successfully to Vercel and Railway, but its required post-merge production checks failed because live Comprehensive runs stopped at `cross_format_truth_verification`.

This was the first incomplete dependency-ordered release package. It remained under Workstream 1 production stability until the complete merge and post-merge evidence chain passed.

## Preserved boundaries

- Preserve the immutable repository, commit, run, evidence-ledger, scanner, scoring, report, and review identities.
- Do not suppress or convert a genuine invariant failure into success.
- Do not alter technical or evidence-adjusted scores to satisfy a gate.
- Rebuild Markdown, HTML, JSON, and PDF only from canonical report truth.
- Keep expert review mandatory and client delivery blocked before approval.
- Do not weaken authorization, security, tenant, secret, or production-change controls.

## Acceptance requirements

- The exact failed final-artifact checks are retained and independently reproducible.
- The root defect is corrected at the canonical source or verifier boundary, not hidden in the UI.
- Focused regressions cover the observed production package.
- All pull-request checks pass on one immutable head.
- No unresolved review thread remains.
- Vercel and Railway identify the merged commit exactly.
- Required post-merge production acceptance, mobile restart, and iOS WebKit proofs pass.
- Two distinct live Comprehensive run IDs reach expert review with report artifacts present and client delivery blocked.

## Verified completion

The package completed on merge commit `4ad64360061c16af0551eb5f55005ed3d3339e76` from PR #968.

- Every pull-request workflow passed on immutable head `b7e16fcee23be64df8741e3f926cb7ed3f1b9cc7`.
- Zero unresolved review threads remained before merge.
- Vercel and Railway both reported the exact merge commit successfully deployed.
- Mobile Restart Production Proof passed on workflow run `30727656163`.
- iOS WebKit Paint Proof passed on workflow run `30727656143`.
- Two-Service Production Acceptance passed on workflow run `30727656129` and retained artifact `8827028960`.
- Live runs `comprun_7cd8f9ca02c04895839c6d38a5b34abf` and `comprun_d34d00ae3a514382aa771d5dc9df6b56` both reached `review_required`.
- Each run retained Markdown, HTML, JSON, and PDF artifacts.
- Each run retained the independently reproducible 92 technical / 90 evidence-adjusted score pair.
- Each run retained nine completed exact-commit scanners, zero incomplete scanners, and 100% analyzer execution coverage.
- Each final canonical package contained 80 findings, with `unique_finding_count`, `finding_register_count`, and `canonical_finding_count` all equal to 80.
- Equivalent source-anchor duplicates and range-bearing repository paths were absent.
- The legacy score contract was reconciled to the canonical score before rendering while prior stale values remained available as reconciliation evidence.
- Expert review remains mandatory and client delivery remains blocked pending approval.

The machine-readable state is maintained in `docs/exact-head-comprehensive-finality-observation.json`. No later work package is declared in the authoritative repository state at this completion point.
