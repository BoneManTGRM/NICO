# Final report atomic single-pass verification

The implementation is not complete merely because unit tests pass. The exact merge must be deployed and produce two distinct public Comprehensive runs that reach expert review.

Required evidence:

- exact Vercel and Railway release SHA;
- final report stage completes without background or request timeout;
- report ID, Markdown, HTML, canonical JSON, and valid PDF retained together;
- final package identity matches run, repository, commit, and evidence ledger;
- existing report structure and design retained;
- one canonical analyzer coverage value;
- no completed analyzer listed as incomplete;
- human review request reached;
- client delivery remains blocked pending approval.
