# Batch A — Express source truth

This batch removes language-mismatched `python_eval_exec` findings from TypeScript/JavaScript evidence, executive repair candidates, and dossier selection; gates unverified high/critical candidates to review-limited; groups unverified secret candidates into one parallel triage workstream; and normalizes repeated CI fallback labels.
