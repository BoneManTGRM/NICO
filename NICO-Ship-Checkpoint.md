# Current continuation — immutable project generated-source snapshots

Continue PR #1641 on `feat/cpp-full-project-capacity`, parent `b609b1c4d490965e1d4954a4b342937712b7a4ea`, tree `9e28e50e5dd50c8913233c5cd267818bbafe7c5b`. Main/base remains `faaa10b037eb58e4175561b96dadf0d929764df4`. This implements the next byte-capture dependency; it is not a complete assessment, production qualification or merge-ready declaration. The containing publication SHA belongs in the PR. C0-C19 and the existing authorization, approval and historical-integrity contracts remain binding.

## Retained fresh hosted baseline

At b609b1c4, integration run `35918375627` completed all five jobs successfully, including baseline job `107379667428`. Artifact `10777407519`: 1,022,895 bytes; ZIP SHA-256 `5b3e4d24724a7bf8ae95ff4dc97ffa7ae1c03a1da4bbdc52d8a2d852b39ca69e`. Downloaded receipt SHA-256 `f25ae91f053c662e0c998162435ac391783c9666178d30b787dc18183bb5db40`. All 21 operation-output hashes were rechecked. The v3 preparation receipt reports BASELINE_EXECUTED; 377 discovered/executed/passed tests, zero skips, 475 retained command contexts; total isolated duration 1,162,711 ms and peak cgroup memory 8,652,029,952 bytes. This does not include the new snapshot stage.

Target remains `bitcoin/bitcoin@bb5296576e8f1a9fc11c19d9a25ba02ed4547e24`, tree `186194c9de7f613d2d323db41cb8ce6bf1e3e549`: 3,248 inventory entries, 3,031 blobs, 49,729,651 source bytes. Contexts retain 445 distinct original and 29 generated files, including one original in two contexts. Database SHA-256 remains `fc89aa69b2a369417cbff01d2784225bd23547827b1f7fd946669f919526aa1b`. Wallet/tests/IPC ON; GUI/benchmarks/ZeroMQ outside the frozen Linux Debug scope. Baseline limits remain 4 CPU, 12 GiB RAM, no swap, 256 PIDs, 9 GiB tmpfs, build 1,200 seconds, tests 720/180 seconds, execution 1,800 seconds plus cleanup. These observations do not qualify later compiler/static/sanitizer workloads.

## First divergence and implemented slice

The shared context adapter retains names and arguments, but generated contents still reside in a mutable build workspace. Extend the existing generated-byte boundary with a separate opt-in project snapshot. The four earlier embedded execution programs and legacy receipt validators remain byte-identical.

`assessment_cpp_project_snapshot` captures every required generated translation unit and inventories regular generated header candidates through descriptor-relative, no-follow reads. Required symlinks, hardlinks, special files, changed inputs, absent members and conflicting paths fail closed. Unfollowed non-header links are explicitly retained. Captured files are placed in a new private analyst-owned read-only snapshot. Header inventory is not compiler visitation; analysis_executed and header_dependencies_verified remain false.

Hard bounds: 2,048 files, 8 MiB per file, 32 MiB aggregate, 40,000 scanned entries, depth 64 and 48 MiB retained stream. Database, original-source population, every distinct context, generated membership and captured file hashes remain bound. The controller reconstructs byte digests and rejects missing/extra members and false analysis claims. No target code executes on the controller.

The existing configuration probe accepts an explicit capture flag only with a baseline contract and artifact sink; opt-in schema v4 adds capture evidence after the successful baseline/JUnit stage. Large bytes are stored once in digest-addressed artifacts, not duplicated in the 16 MiB receipt. Native outcome is retained before storage or parsing; a failed capture/storage/identity check remains UNPROVEN while retaining the successful build/tests. Production selection and normal completion receipts are unchanged.

The existing workflow now runs owned positive and negative controls before Bitcoin. Their small source includes an original compiled in two contexts, a generated C++ source and generated header; the negative adds an explicitly rejected header symlink. The control freezes its measured database, builds and tests under the same boundary, reparses the retained snapshot and verifies exact template/output bytes. The Bitcoin baseline opts into capture only after those controls. No new workflow, permission, service, tool image, source revision, job deadline or workload limit was introduced.

## Verification and invalidation

Preserved RED evidence: 23 missing-capture cases; nine native/sink/workflow integration cases; one missing-control case; four omitted/mismatched context-binding cases. They are separate stages, not additive coverage counts. The new suite contains 37 cases. The complete local workflow contract command produced a terminal summary of **698 passed in 72.30 seconds**. The caller timed out before that child command completed; its retained stdout contains the full terminal pytest result, but no shell exit marker was recovered. A fresh focused command completed with exit zero: **108 passed in 3.49 seconds**. Earlier truncated local broad-suite output remains incomplete and is not counted as a pass.

AST parsing, every workflow shell block, unchanged per-job/dependency/permission/concurrency comparisons and git diff --check pass. All four legacy embedded program hashes match the preserved pre-change manifest. The owned fixture itself configured, compiled and passed its native test locally with GCC 14.2; no local Docker/isolation or Bitcoin snapshot proof is claimed. The real hosted positive/negative capture controls and Bitcoin v4 snapshot must still be inspected after publication.

Three risks checked: generated/source/context substitution; unsafe or changing filesystem inputs; treating captured files or retained success from an earlier stage as analyzed coverage. Corresponding negative tests retain failing evidence rather than suppressing it. This is author verification, not independent review. No baseline rerun was manually dispatched; the necessary producer/workflow mutation triggers normal candidate CI.

## EXACT NEXT ACTION

Inspect this candidate's automatic contract/control/Bitcoin jobs. Verify native snapshot bytes, exact generated membership, all captured hashes and artifact references, and preserve any first failure before repair. Do not rerun unchanged failed or successful jobs without new evidence.

Then use the private snapshot for every required compiler/static-analysis context, retaining actual header-dependency and analyzer completion evidence. Capturing bytes does not yet make the full analysis path runnable. Complete the declared functional/sanitizer/fuzz scope and qualified durable production worker/report integration, independent complete review, final checks, gated merge, exact frontend/backend/worker deployment and actual authorized English/es-MX Bitcoin report and repeated retrieval. C6/C7/C11 retain baseline subproofs only; no whole C0-C19 PASS follows from this slice.

Independent-review request `5802036311` received bot reply `5802038248` at 2026-09-23 20:03:01 UTC reporting its allowance exhausted; no independent review occurred. Do not retry unchanged requests, buy credits, change accounts or waive review. This limits that review gate, not permitted implementation. No merge/deployment, activation, new privileges/spending, automated human review/approval or client-delivery authorization occurred. Existing Railway staged changes are untouched.

## Immutable predecessor

The full predecessor is `b609b1c4d490965e1d4954a4b342937712b7a4ea:NICO-Ship-Checkpoint.md`, blob `8cf4e279a5df7e51a49c1e80cc8d3583cd15a892`, SHA-256 `6528a95dbf2176b366fe812eea42d6748052db5cb716aa724ca0690cbabf8efe`. Its d236/a10/abb/1ebc chain preserves earlier successful and failed Bitcoin runs, PR1627, worker/database/production evidence, historical approvals and the full C0-C19 contract. This remains the only active ledger. Do not declare SHIPPED before every mandatory acceptance predicate is proved.
