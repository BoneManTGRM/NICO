# Current continuation — repair generated-byte capacity and exact security disposition

Continue PR #1641 on `feat/cpp-full-project-capacity`. Parent `fd1e93aea4b644ac3b6b243d846800eac42a25dc`, tree `08c14ba8e5155f8a2d0c22ba4ecd91cc75c43cc7`; main/base `faaa10b037eb58e4175561b96dadf0d929764df4`. Record the containing publication SHA in the PR rather than making a self-hash commit. The original C0-C19, authorization, independent review, production and human-approval requirements remain binding. This repair is not full-assessment completion.

## First retained failures and bounded corrections

Native integration run35929218470, baseline job107416764490: FAILED after a successful build and all native tests. Retained artifact10781657379 is 1,027,099 bytes, ZIP SHA-256 `5e406360bfcb8839922aa5cc6aa009b3fca22fcc0ca0359bdd659b8ba3079e84`. Its v5 receipt preserves compiled=true and tests_passed=true, with build845,050ms, tests296,428ms, total1,157,334ms, peak8,652,402,688 bytes, boundary and cleanup verified. Generated capture exits1 with `worker_generated_type_or_size_invalid`; generated_context=null and project_compiler=null. No compiler context ran. A failed capture is not a Bitcoin compile failure or successful full assessment.

There is a demonstrable capacity mismatch: the frozen source contains a 1,582,208-byte raw embedded network-map asset and the actual cache has WITH_EMBEDDED_ASMAP=ON. The exact upstream GenerateHeaderFromRaw.cmake expands every byte to sixteen source characters and inserts a newline per eight bytes: the resulting header body alone is 25,513,104 bytes, excluding its small wrapper. This cannot fit the existing 8 MiB per-file snapshot reader. The retained generic failure does not name its first offending path; the correction's native result must still establish whether other capture issues remain.

Raise only the project-generated per-file read ceiling to 32 MiB, sharing the existing 32 MiB aggregate ceiling. Keep the 48 MiB encoded output cap, 2,048-member limit, 40,000-entry traversal cap, depth64, no-follow/single-link/stability checks and private read-only snapshot unchanged. Make the compiler's generated-input verifier use the same constant, including its standalone embedded program. Do not change the earlier header-only programs, original-source bounds, executor/job timeouts, CPU/RAM/PID/network boundaries, tool/source/configuration identities, or workload. Do not remove any generated member or disable embedded data. A synthetic 25,513,104-byte header reproduces the old reader failure and is captured byte-exactly by the correction; oversized files and aggregate exhaustion still fail closed.

Security run35929218347, job107411563750: its retained gate has exactly one blocking unapproved Trufflehog candidate. Artifact10780830520 is179,858 bytes; ZIP SHA-256 `a73fa952532a912664f930cd7503c851584ad8efff53fd2f29b6e5a64ba76722`. The candidate is in NICO-Ship-Checkpoint.md, DetectorName=RailwayApp, Verified=false. Its raw digest is `ab6604b5617b61f4670629e75f542f1d93c433174affb467e76d73a5eda1d642`. A fresh authenticated Railway deployment lookup identifies that exact value as the SUCCESS production deployment for main faaa10b0, not an authentication token.

Extend the existing exact checkpoint/detector/Verified-is-False disposition with that one digest. All101 native Trufflehog observations and their original bytes remain retained; verified findings, different values, paths and detectors still block. Replay of the unchanged native audit inputs now has zero blockers and retains all existing review-required populations, human_review_required=true and client_delivery_allowed=false. This is local replay of actual scanner evidence, not a new hosted security PASS or a human disposition of the remaining findings.

## Verification

Focused reproduction: four expected RED failures (large capture, compiler bound, exact nonsecret disposition and mixed-population classification),42 passes. Identical repaired command:46 passed, exit0. Final affected snapshot/compiler/context/configuration/baseline/schedule/security command:231 passed in5.06s, exit0. Counts overlap; do not add them. Synthetic boundary cases include over-limit file/aggregate, byte identity, exact nonsecret matching, verified/unknown statuses, wrong detector/path/value and mixed populations.

The broad local workflow command timed out with partial output; no completed broad local pass is claimed. The attempted interactive test facility is unsupported. No active pytest child remained afterward. The complete hosted regression job is still required; do not repeatedly rerun the same broad local command. AST checks,13 workflow shell blocks and git diff --check pass. Parsed workflow comparison shows only registration of the new regression suite; all execution scheduling and limits remain unchanged. The local Git initialization is a source fixture, not an upstream release.

Three repair risks checked: capture versus compiler cap drift; weakening aggregate/isolation/membership limits; exempting an unknown or verified secret. Corresponding tests and native audit replay preserve the boundaries. No independent review or actual new Docker/Bitcoin run is claimed by these local checks.

## Retained baseline and exact next action

Bitcoin remains `bb5296576e8f1a9fc11c19d9a25ba02ed4547e24`, tree `186194c9de7f613d2d323db41cb8ce6bf1e3e549`. Retain475 contexts:445 original and29 generated distinct files, one original compiled twice;377 discovered native tests. Frozen database `fc89aa69b2a369417cbff01d2784225bd23547827b1f7fd946669f919526aa1b`. Linux Debug wallet/tests/IPC stay ON; GUI/benchmarks/ZeroMQ remain outside the previously frozen scope. The earlier complete baseline success is unchanged historical proof, not compiler/static/production proof.

Publish this bounded repair atomically on the existing branch after rechecking its head; inspect the resulting native capture/compiler and security outcomes. Verify generated member bytes/hashes and every required compiler context; preserve the first actual failure and diagnose it without silently dropping targets or blind retries. No manual duplicate Bitcoin run is needed. New capture-capacity behavior invalidates its dependent snapshot/compiler proof, not historical baseline results.

Then complete configuration-aware static analysis and findings, declared functional/sanitizer/fuzz scope, qualified durable production worker/profile/report integration, independent complete review, final checks, gated merge/deployment, exact frontend/backend/worker release mappings and the authorized English/es-MX Bitcoin Comprehensive report with repeated retrieval. Production remains inactive and unmerged. No operator approval, client-delivery authorization, spending change, new permission, or unrelated Railway staged change is performed.

The recorded independent-review request5802036311 received bot reply5802038248 reporting exhausted review allowance; no independent review occurred. Do not retry unchanged requests, buy credits, switch accounts or waive review. That gate remains separate from permitted implementation. Do not declare SHIPPED until every mandatory C0-C19 predicate is proved.

## Immutable predecessor

Complete predecessor: `fd1e93aea4b644ac3b6b243d846800eac42a25dc:NICO-Ship-Checkpoint.md`, blob `ebe3c540be61273e60c8ce9066d30015003ae68c`, SHA-256 `f5b23409359b7a4c0b665c6ebf9bd04e5311a662fcdecad6be6726a423bed85c`. Its2543801b/b609/d236/a10/abb/1ebc chain retains earlier successful and failed native results, exact source/tool/control identities, original C0-C19, PR1627, worker/database/production and historical approval evidence. This remains the only mission ledger.
