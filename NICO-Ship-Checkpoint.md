# Active mission: Comprehensive C/C++ and Bitcoin qualification

Continue PR #1641, `feat/cpp-full-project-capacity`, under the original NICO-CPP-Bitcoin-Execution-Prompt.md and C0-C19 contract. Same-branch non-force publication and gated normal release are authorized. No replacement PR, invented spending/privileges, platform bypass, scope reduction, automated human approval, or unsupported SHIPPED claim.

## Immutable continuity — 2026-09-23

Publication parent `d7f43b2e650f2048cf7917d2684a772cd3049d2b`, tree `165948b33a662f6b9ecea99db0c4938369146c7b`; observed main/base `faaa10b037eb58e4175561b96dadf0d929764df4`. Resolve the enclosing/newest SHA through GitHub, without an extra self-referential documentation commit.

The full preceding checkpoint remains at [d7f43b2e](https://github.com/BoneManTGRM/NICO/blob/d7f43b2e650f2048cf7917d2684a772cd3049d2b/NICO-Ship-Checkpoint.md), blob `383ac67c094e93849f0b4bdf9634b0198e0c6d8e`, SHA-256 `1d544b569e4a14772bfcc3e2dda9e0ee989bd842a8ad59e2caeb28de2a38265d`, 6,627 bytes. It preserves the complete 59/a40/b08/370/934/0b9/PR1627 chain, C0-C19 definitions, prior failure outcomes, worker/database/registry evidence, authorization/restrictions and historical approvals. This remains the sole active mission ledger.

## Published repair now passes actual hosted native integration

All 21 PR workflows on d7f43b2e were observed terminal SUCCESS. Actual C++ integration run `35875784741`, native job `107233506636`, and its three prerequisite/source/inventory jobs passed. No rerun or gate relaxation was needed.

Downloaded and SHA-256-verified source artifact `10756963042`: ZIP `eef03139fdf7b1b5b0b099da5248bc22118329d8854d8b00d6e5aef790e45095`; inner source archive `a0dbebaea21b0128d628dca37513a6481ba991745ca47522c048eb8737e45182`. Its CI merge `6bd217dd86070fd657cd3082c15f0271bf9420fe` has the exact parent tree. This is the exported source subset, not a full local GitHub checkout or production release.

Native artifact `10757682910`: 646,001 bytes; verified ZIP `cbac71d3bc8cc9f86f92ff574db25b567bf4a7fb9789788091216a4eb48d2d02`. Actual result `PASS_OWNED_PROJECT_INTEGRATION`, 50,415 ms, two controls; synthetic issuer true, production dispatch false, Bitcoin executed false, production qualified false. Mixed C/C++ configuration/build, original/generated/image-header evidence, retained compiler options, Boost/SQLite/Libevent behavior, sanitizer replay and bounded fuzz passed the existing native assertions. Earlier failed 59 run/artifact remains failed. No new PDF visual inspection or human approval is claimed.

## CONFIGURE-FIRST-1: prepare the real large-project population, not another toy-only proof

The existing full-project runner requires frozen translation-unit/test populations. A real large CMake project must first establish its configuration/compile database; inventory counts or small controls cannot supply that plan. Added reusable configure-first preparation using the existing isolated worker primitives. Existing `assessment_cpp_full_project_execution.py` and owned integration producer remain byte-identical to d7f43b2e; no replacement receipt/report/persistence path or production selector is introduced.

A bounded read-only Git inventory establishes the supplied exact commit/tree and all entries, verifies every materialized original regular file against its Git blob plus SHA-256, preserves executable modes and atomically stages only original files. No target command, install hook, fetch, submodule operation or target executable runs on the controller. Gitlinks, symlinks and LFS pointers remain explicit exclusions; C/C++ symlink/LFS source is rejected rather than silently omitted. There is no sampling, and Git history never enters the executor.

CMake configuration executes only after the existing root-owned/read-only source provisioning and native boundary checks, in the same digest-bound nonroot/no-new-privileges/no-capabilities/no-external-network container. Exact compiler/CMake versions are checked. Capture retains bounded native command output, CMakeCache and compilation-database bytes/hashes, original versus generated unit membership, invocation counts, memory peak or unknown, and verified cleanup. Requested options must match recognized typed cache entries; an unused UNINITIALIZED -D value cannot be accepted as effective scope. Cache declarations remain target-produced observations, not proof of semantic feature behavior.

Every returned operation is persisted before later interpretation. Partial/failure remains UNPROVEN. CONFIGURATION_CAPTURED is preparation only, never analysis/build/test/sanitizer/fuzz completion, worker authority, or production readiness. CMake may itself compile/run feature probes inside isolation; the `compiled=false` field means the project build stage was not executed, not that configuration performed no compiler operation.

## Fixed benchmark scope and budgets before execution

Fixture-only target: `bitcoin/bitcoin@bb5296576e8f1a9fc11c19d9a25ba02ed4547e24`, tree `186194c9de7f613d2d323db41cb8ce6bf1e3e549`; recorded full inventory 3,248 entries, 3,031 blobs, 49,729,651 source bytes. Historical Bitcoin revision/run remain separate. Repository-specific identities/options occur only in the benchmark fixture/workflow, not production selection rules.

Proposed meaningful Linux x86_64 baseline keeps wallet and native tests enabled: BUILD_TESTS=ON, ENABLE_WALLET=ON; GUI, benchmarks and ZeroMQ are outside this baseline (BUILD_GUI=OFF, BUILD_BENCH=OFF, WITH_ZMQ=OFF). Debug CMake/GCC configuration is fixed. These exact option names/values require actual typed-cache confirmation; failed or unused options are not silently removed. This configure stage does not freeze away the mandatory later address/undefined sanitizer or bounded-fuzz scope.

Unchanged 2 CPU / 2 GiB / 256 PID isolated class, zero swap, bounded writable work area. Source freeze deadline 20 seconds, 16 MiB per original blob and 64 MiB aggregate; Git metadata output 8 MiB. Probe execution budget 80 seconds, configure command at most 50 seconds, total advertised bound 90 seconds including memory/cleanup allowances. Compile database 4 MiB, cache 1 MiB, overall retained preparation evidence 16 MiB. Existing native CI job stays five minutes and contents:read only. The probe reuses its already-built image after successful owned control; no additional image build/registry publish, unlimited retry or spending grant.

## Verification and invalidation

Missing-capability RED: 11 configure cases; then three full-inventory/workflow cases. Effective-cache RED: two expected failures (unused option accepted and missing cache identity), followed by all 16 focused preparation cases passing. Final affected command: 93 passed in 13.59 seconds, including existing capture/mixed-language/options/fixture/schedule neighbors. Python AST, every workflow shell block, unchanged read-only/five-minute limits and diff whitespace checks pass. Existing runner and owned producer are byte-identical; their native parent proof remains scoped to original inputs.

The complete configured contract command then passed all 592 tests in 138.67 seconds, terminal exit 0, including every existing exporter without exclusions. This overlaps the 93-case result; counts are not additive. Local Python 3.13.5 differs from hosted Python 3.11.

Local Docker is unavailable. The new real target checkout, complete inventory and isolated configuration must be established by the newly published hosted run, not local mocks. The new workflow requires the preparation tests and retains its artifact even after failure. Parent green CI is not new-candidate proof. Author validation is not independent complete review.

Three reviewed risks: original-byte substitution or unsafe paths; assessed execution/credentials on the controller; preparation or ignored option misrepresented as a completed assessment. The boundary tests and explicit partial states address these risks without weakening existing assertions. New benchmark/configuration evidence and full candidate CI are invalidated by this increment; existing unchanged native controls and historical evidence remain valid only within their original scope.

## Acceptance and EXACT NEXT ACTION

C0/C1/C2: parent control qualified; frozen actual Bitcoin inventory/configuration preparation now wired but hosted results pending. C3/C4 worker authority/durability subproofs retained, not promoted to final production. C5/C6/C7/C11 Bitcoin required static/build/header/test/sanitizer/fuzz and measured aggregate resources still unqualified. C8/C9/C10 canonical populations/score protections unchanged; configuration counts are not analyzed coverage. C12 existing controls retained; new candidate checks required. C13/C14/C15 actual bilingual report/mobile/approval acceptance outstanding; human-only actions stay human-only. C16 independent full review and material-finding disposition still required. C17/C18/C19 no merge/deployment/production Bitcoin report/repeated-retrieval acceptance claimed.

Publish on the same PR, inspect the actual frozen-source/configuration receipt, and repair the first evidenced failure without changing the target, dropping required options/units or weakening isolation. Use the returned real compile database/cache to finish the original meaningful large-project execution contract and implementation. Preserve failed outcomes and only repeat invalidated proof. Complete independent base-to-final-head review, qualified production profile, and all pre-merge gates before merge; then verify exact frontend/backend/worker serving identities and the actual authorized bilingual Bitcoin report. No old discovery refusal is retried, rephrased or delegated to evade a genuine boundary, and no human approval is automated. The final SHIPPED declaration remains unavailable until every mandatory C0-C19 predicate is evidenced.
