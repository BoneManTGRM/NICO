# Current continuation — source-bound project compiler and header replay

Continue PR #1641 on `feat/cpp-full-project-capacity` from parent `2543801b683c2277d4f7704f148b809f70037d87`, tree `0ea1f94be14fc8af995f2fcd10b2b99f42bb7ca9`. Main/base remains `faaa10b037eb58e4175561b96dadf0d929764df4`. The containing publication identity belongs in the PR. This is the compiler/header dependency, NOT complete static analysis, production qualification or merge readiness. The original C0-C19 and all authorization/approval/history boundaries remain mandatory.

## First missing layer and implemented slice

The parent captures generated bytes but has no consumer that checks every original/generated/repeated compilation context against that private snapshot. The original compiler replay also rejects legitimate flags in the retained Bitcoin database. The new opt-in `assessment_cpp_project_compiler` preserves every context, remaps generated inputs/include directories into the existing private snapshot, retains argument order and identities, and runs GCC syntax checking with native dependency-file capture. It produces no objects and executes no target binary. The already verified full build remains a separate requirement and is not replaced by syntax checking.

The existing compiler allowlist remains unchanged. A separate bounded adapter accepts the observed GCC 14 scalar forms: overflow trapping, extended identifiers, control-flow protection, bidi diagnostics, selected x86 instruction flags and tightly scoped macro-prefix maps. It still rejects plugins, wrappers, response files, helper/spec paths, arbitrary files and unknown options. A fixed macro-prefix mapping preserves generated source macro identity after relocation. Compiler diagnostics are not security findings or mitigation proof.

Every record retains context identity, exact derived invocation, native exit/timeout/truncation/duration, dependency bytes and hashes, and original/generated/toolchain dependencies separately. The controller reconstructs attempted and checked context populations and header inclusions. A copied but unvisited header remains explicitly unvisited. Failed, timed-out, substituted, malformed or missing evidence receives no completed coverage. `static_analysis_executed`, `object_code_generated` and `production_qualified` remain false.

The existing configure probe adds opt-in preparation schema v5 after successful baseline tests, JUnit and validated generated capture. Compiler output is retained once in the existing digest-addressed artifact sink. Native/storage/identity failures preserve successful earlier stages and leave overall status UNPROVEN. The regular production completion validators and production profile selection are untouched.

New compiler pass bounds: 540 seconds aggregate, 90 seconds per context, at most four concurrent compiler processes, 48 MiB retained output, 16 MiB request, 1 MiB per native output file. It stays inside the existing 1,800-second executor and the existing 4 CPU/12 GiB/no-swap/256 PID/9 GiB scratch boundary; no original workflow deadline or workload budget increases. Sufficiency for all Bitcoin contexts is UNPROVEN until measured. The owned positive and negative snapshot control now requires real compiler/header evidence on the positive branch before Bitcoin can begin. The negative capture case must still fail before compiler execution.

## Fresh verification, limitations and three-risk check

- Initial missing compiler/header behavior: 33 RED failures. Missing probe/artifact/workflow connection: three RED failures. Counts describe successive stages, not additive coverage.
- Final focused compiler/snapshot/context/configuration/baseline/capture/schedule command: **170 passed in 10.09 seconds**, shell exit zero. The new compiler suite contains 49 cases; counts overlap.
- A real owned GCC 14.2 smoke test checked three contexts (one original twice and a generated `.c++` unit) using the private copied header. Native dependency files included the actual visited header and excluded an unused header; no object or target execution occurred. A standalone relocated version of the embedded collector also completed three actual GCC calls under UID 1001. This local smoke test changes only test paths/tool locations and is NOT Docker/cgroup/Bitcoin/production qualification.
- The broad local workflow command was interrupted at its container command deadline with only partial dot output and no exit marker. It is NOT a full-suite pass and is not converted into one. Complete hosted regressions remain required.
- All workflow shell blocks, Python syntax and diff checks are required before publication. Original job limits/dependencies/permissions/concurrency and the four legacy embedded compiler/snapshot programs are unchanged.

Three reviewed risks: command-option or path substitution; collapsing repeated/generated contexts into file counts; awarding header coverage after failed or incomplete execution. The new tests cover each, including native/storage failures retaining the passing baseline. This is author verification, not independent review.

## Parent execution evidence preserved

Parent run `35927064006` has completed its contract job and owned integration job `107406714554` successfully, including native positive/negative generated-snapshot controls. Its Bitcoin baseline/capture job `107408261823` was in progress at the last observation; no Bitcoin v4 capture success is inherited without its receipt. Source artifact `10779601732` is 4,421,460 bytes, ZIP SHA-256 `85adac7b0281e0faa54312872bbf810d03d11345239614e70cb51524ec5d4efa`; archive checksum was verified. CI merge snapshot `be2c40b4d0e58543cb9b26db4ccf9bf226aa7472` has the same tree as parent2543801b. A local Git fixture from those bytes is NOT an upstream parent and must never be pushed.

Previously verified b609b1c4 run `35918375627`, baseline job `107379667428`, artifact `10777407519` remains valid for its unchanged inputs: BASELINE_EXECUTED, all 377 native tests passed, zero skipped; 475 contexts, 445 original and 29 generated distinct files. ZIP SHA-256 `5b3e4d24724a7bf8ae95ff4dc97ffa7ae1c03a1da4bbdc52d8a2d852b39ca69e`; receipt SHA-256 `f25ae91f053c662e0c998162435ac391783c9666178d30b787dc18183bb5db40`.

Bitcoin remains `bb5296576e8f1a9fc11c19d9a25ba02ed4547e24`, tree `186194c9de7f613d2d323db41cb8ce6bf1e3e549`; 3,248 entries, 3,031 blobs, 49,729,651 source bytes. Database SHA-256 `fc89aa69b2a369417cbff01d2784225bd23547827b1f7fd946669f919526aa1b`. Linux Debug wallet/tests/IPC stay ON; GUI/benchmarks/ZeroMQ remain outside the previously frozen scope. No changed target, tool image, baseline work or source population is authorized by this increment.

## EXACT NEXT ACTION and release boundary

Publish the verified slice as one non-force descendant on this same PR after checking current head. Inspect automatically triggered full contract checks, owned positive/negative controls, and the frozen Bitcoin baseline/snapshot/compiler receipt. Verify every context/dependency/artifact hash and preserve any first failure before repair. Do not dispatch duplicate runs or weaken limits/required populations merely to get green.

Next connect verified per-context compiler/header evidence to actual configuration-aware static analysis, preserving Cppcheck outcomes/findings and limitations. Then finish declared functional/sanitizer/fuzz scope, qualified durable production worker/profile/report integration, independent complete review, final checks, gated merge, exact serving frontend/backend/worker release and authorized English/es-MX Bitcoin Comprehensive reports with repeated retrieval. No whole C0-C19 PASS follows from the new code or the old baseline.

Independent-review request5802036311 received bot reply5802038248 at 2026-09-23 20:03:01 UTC reporting exhausted allowance. No independent review occurred. Do not retry unchanged requests, buy credits, change accounts or waive review. This limits review, not independently permitted implementation. No merge, production deployment/activation, new privileges/spending, automated human review/approval or client delivery authorization occurred. Existing Railway staged changes remain untouched.

## Immutable predecessor

Complete parent checkpoint: `2543801b683c2277d4f7704f148b809f70037d87:NICO-Ship-Checkpoint.md`, blob `2559342bdfacbe63800103342e06f66b7041b5bf`, SHA-256 `7b2c3ae30af65c243fdb2926ac0d4bbc92f3aa4d0c4924b795661e0d4e3ad2f3`. Its b609/d236/a10/abb/1ebc chain preserves all prior accepted and failed runs, source/tool/budget changes, the original C0-C19 contract, PR1627, worker/database/production evidence and historical approvals. This remains the sole active mission ledger. Do not declare SHIPPED without every mandatory predicate.
