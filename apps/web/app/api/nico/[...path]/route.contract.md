# NICO proxy recovery contract

This route remains allowlisted to native assessment lifecycle and bounded diagnostic endpoints. Production backend retry behavior is bounded, HTTPS-only, credential-free, and fail-closed. Request retry metadata is diagnostic only and cannot authorize client delivery or change assessment evidence.